#!/usr/bin/env ruby

require 'pry'
require "bundler/setup"
require_relative "../lib/mtg_cardseeker"

#The file that the user will run. is this the gem?


# response = HTTParty.get('https://api.magicthegathering.io/v1/cards')
# body = response.body
# @data = JSON.parse(body)

MtgCardseeker::CLI.new.start